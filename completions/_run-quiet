#compdef run-quiet

_run-quiet() {
  if (( CURRENT == 2 )); then
    _alternative \
      'options:option:(-h --help)' \
      'commands:command name:_command_names -e'
  else
    words=("${words[2,-1]}")
    (( CURRENT-- ))
    _normal
  fi
}

_run-quiet "$@"
